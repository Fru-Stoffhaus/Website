# Password for the banner admin page at /admin
ADMIN_PASSWORD=change-me

# Vercel KV (Upstash Redis) — auto-injected when you add the integration on Vercel.
# Either the KV_* or UPSTASH_* pair works.
KV_REST_API_URL=
KV_REST_API_TOKEN=
